// SQLite-based DB initializer has been removed in favor of MongoDB (Mongoose).
// If you still need to migrate legacy data from `music_app.db`, run the
// migration scripts that read the SQLite file directly and write into MongoDB.
// This file intentionally no longer initializes or exposes a SQLite DB connection.

module.exports = null;

